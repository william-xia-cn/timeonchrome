# DECISIONS

| ID | 决策 | 状态 | 说明 |
|---|---|---|---|
| D-001 | 项目进入 V0 收口阶段 | Active | 以稳定与收口为先，限制扩散开发 |
| D-002 | music time 纳入 V0 | Active | 作为当前阶段功能范围的一部分 |
| D-003 | composite routing 延后到 V1 | Active | 当前不进入实现，避免打断收口主线 |
| D-004 | monitoring 收口按小包推进 | Active | 已完成核心用户可感知副作用收口 |
| D-005 | 临时工具仅 dev-only | Active | 不接正式导航、不产品化 |
| D-006 | V0 冻结能力已具备 | Active | 依据：全量逐文件 unit tests passed、audioSeconds 口径稳定 |
| D-007 | cloudHeartbeat 必须保留 | Active | 已定产品决策；不作为 V0 缺口，不进入删除/短路讨论 |
| D-008 | V0 发布闸门需在可执行环境复验 API/E2E | Active | 本地闸门若受网络/浏览器环境限制，需在 CI/发布机完成最终放行 |
| D-009 | Codex 成为默认代码执行器 | Active | Codex 本地部署完成，接管日常代码执行；OpenCode 降级为本地复验/环境排查/fallback；Antigravity 从协作链路移除；OpenCode timing trace diagnostics 工作已收口并推送（commit `71516d2`） |
| D-010 | 跨自然日计时按自然日切分 | Active | `event_log_v1` 中跨午夜的未闭合/闭合区间在统计“今日时长”时应按自然日边界切分，不应全算入 START 日或 END 日 |
| D-011 | 凌晨休息时间限制进入后续产品设计 | Active | 后续需要支持配置“凌晨不可用于休息时间”的时段策略，用于防止熬夜玩游戏；该能力属于配额/策略层，不阻塞当前计时准确性收口 |
| D-012 | PiP 不属于正常学习需求 | Superseded by D-041 | 切换到学习模式时，已经打开的非学习网站 PiP 必须关闭；PiP 视频时长需要单独记录，不混入普通在线/ACTIVE 时长；产品态度是记录但不作为学习需求放行 |
| D-013 | 后台媒体与 PiP 统计必须保留 domain 维度 | Active | 后台 audio/video 与 PiP 时长不能只有全局总秒数，必须能按 domain 归因；总量可作为摘要，但不能替代 domain 明细 |
| D-014 | Agent 必须严格遵循已确认的实施方案 | Active | 已确认方案必须严格逐项执行，不得擅自简化、替换或偏离；执行前须输出 checklist，执行后须做 Plan Conformance Audit；存在 Deviated/Extra 变更时禁止提交 |
| D-015 | 申诉/审核语义：使用分析页面 / 终端 UI 待定 | **Pending PO** | 复合型网站会话的"申诉/待审核/申诉中/已改判/标为学习/标为休息"等概念在**孩子侧终端 UI（admin 管理面板 / 使用分析页面）**中仅为**占位展示**，尚未获得产品终审。当前存在不等于产品批准。Codex/OpenCode 禁止将其扩展为新的交互工作流或新增业务逻辑。Product Owner 后续需决定：a) 从终端 UI 隐藏；b) 仅作只读状态展示；c) 保留孩子侧申诉操作；d) 仅保留在家长控制台 |
| D-016 | Vendor/Support 域名保留在 defaultCompositeSites | Active | 以下 3 个域名明确保留在 `defaultCompositeSites`（系统配置复合网站）中：`microsoft.com`、`apple.com`、`adobe.com`。这是 Product Owner 明确批准的例外，不适用于其他软件/vendor 域名。禁止将其移动到 `defaultStudySites`，禁止以此为理由继续添加同类域名。新增软件/vendor 域名到系统默认清单必须获得 Product Owner 单独批准。 |
| D-017 | System Recovery 场景是 V0 formal release gates | Active | Chrome close/reopen、Lock/unlock、OS sleep/wake、Network offline/online 是 V0 formal release 的 Release Gates，不是普通回归测试。V0 formal release 前这些 Gates 必须通过，或由 Product Owner 明确 waive。runner 实现不等于 Gate pass；SKIP 也不等于 Gate pass。OpenCode 停止作为主执行器，Codex 接管剩余 Release Gate 工作；Chrome Web Store 发布是独立后续任务，不属于当前 Gate 范围。RG-1~RG-4 最终状态以 Product Owner confirmed 为准并已在 `PROJECT_MASTER.md` / `TASK_BOARD.md` 落档。 |
| D-018 | Time Accounting Reliability Model | Active | V0 计时统计采用统一 stale-boundary 规则：有效计时只能来自闭合事件段；若 `now - lastHeartbeat > STALE_GAP_THRESHOLD`，旧段必须在 `lastHeartbeat` 关闭，否则在 `now` 关闭；关闭时间必须 clamp 到 `[session.startTime, now]`，新段若存在从 `now` 开始。`lastHeartbeat` 到 `now` 的未观察 gap 不计入普通 foreground、后台 audio 或 PiP 有效时长；badge 当前段显示也必须使用同一封顶口径。不迁移历史 `event_log_v1`，聚合 schema 保持兼容。 |
| D-019 | V0 三模式低摩擦切换 UX 规格冻结（文档决策） | Active | V0 可见模式定义为 Study/Composite/Rest/Paused；Composite 使用独立时间归因与配额，不并入 Study。切换规则：Study→Composite 自动切换 + 45s 轻提示（非阻断确认页）；Rest→Composite 自动切换+轻提示；Study→Rest 必须单轴横向滑动确认（不可单击）；Composite→Study 与 Rest→Study 自动回归；Composite→Rest 普通确认；hardBlocked/unsafe 走独立拦截流，不参与模式切换。非目标：AI 分类、composite 二级分类、path-level routing、import/export schema 变更、复杂手势解锁。并保持 `SITE_ACCESS_POLICY.md` 边界（compositeList 窄口径；普通门户/社交/游戏不默认进 composite；`bilibili.com` 维持非 Study/非 Composite/非 unsafe，归受限娱乐网站，除非 PO 后续改判）。 |
| D-020 | V0 自动模式切换稳定门控 | Active | 自动切换增加门控防抖：`Rest->Composite` 需目标站点前台活跃连续 30 秒；`Rest->Study` 与 `Composite->Study` 需前台活跃连续 45 秒。门控期必须监控开启且有用户活跃，若切站/中断/监控关闭则取消候选并重新计时。门控期时间归属保持原模式，不做回填。显式确认切换（Study->Composite、Study->Rest、Composite->Rest）保持即时生效。 |
| D-021 | Admin CSP 告警当前按已知问题管理（未修复） | Active | `admin/admin.html?view=stats` 的 `Executing inline event handler violates Content Security Policy` 控制台告警在多轮诊断后仍未定位到唯一可行动源。当前口径：不宣称 fixed，按 admin 页面已知告警管理；不得将其直接判定为核心 runtime/mode/popup/content-script 失败。需继续以功能验证为准（stats/rules/devices/navigation/login/logout/save/sync）；本决策不改变 V0 formal release gate 状态。 |
| D-022 | V0 closeout 采用“手动验收 + gate 放行”双轨判定 | Active | Product Owner 已手动验收通过 Reminder UI、pending mode-transition feedback、Popup V0 layout、Admin 功能；但 Formal V0 Release 仍需满足发布闸门：System Recovery Gates、macOS smoke checklist（`docs/macos_v0_smoke_test_checklist.md`）、回归测试与打包前校验。任一 gate 未完成时，不得宣称 V0 release-ready。 |
| D-023 | Product Owner 接受 V0 验证缺口发布风险（转 V1 跟进） | Active | Product Owner 确认：为加速 V0 release/Chrome Web Store handoff，接受以下风险在 V0 不阻塞发布推进：1) macOS smoke validation deferred to V1（未通过/未完成，不得宣称 passed）；2) Playwright E2E alternate-environment evidence deferred to V1（Windows 本地 `spawn EPERM` 环境阻塞，未完成，不得宣称 passed）。影响：两项从 V0 active blockers 转为 V1 follow-up，必须保留在 known issues / release notes；Admin CSP 告警仍按 D-021 作为未修复已知 warning 管理。 |
| D-024 | Composite 配额耗尽采用统一拦截流 | Active | Composite 与 Rest 配额池独立。Composite 耗尽时禁止继续 Composite、禁止借用 Composite、禁止自动占用 Rest。统一行为覆盖 Study→Composite / Rest→Composite / Composite→Composite / 同日临时综合放行继续访问：若 Rest 仍可用，仅提供显式“进入休息继续”；若 Rest 也耗尽，仅允许“返回”。“返回”不得切换模式或消耗配额。 |
| D-025 | V0 借用/申请入口与站点类型约束 | Active | Restricted Entertainment 不允许申请 Composite 时间；Unclassified 允许同日申请 Composite 且在 Rest 配额耗尽时允许借用 Rest；Popup 不提供借用或 Composite 申请入口；HardBlocked/Unsafe 不允许借用/申请/临时放行。 |
| D-026 | V0 Rest→Unclassified 与返回语义落档 | Superseded by D-048 | 历史规则：Rest 模式访问 Unclassified 且 Rest 配额可用时，保持 Rest 且不弹提醒；不自动转 Composite，不自动触发综合申请。该规则已被 D-048 取代：未归类网站是待归类入口，不再作为普通 Rest target。 |
| D-027 | site-access 示例文件路径单点化 | Active | V0 仅保留 `docs/site-access-config.example.json` 作为示例路径；不维护根目录同名重复文件。该决策不改变 schema，不增加临时综合放行或 Rest 借用状态到 import/export。 |
| D-028 | 不自动迁移现有 Profile 接收 defaultUserCompositeSites | Superseded by D-052 | 历史规则：`defaultUserCompositeSites`（用户默认复合网站）仅在新建 Profile 时一次性种子注入 `customCompositeList`。该规则已被 D-052 取代：系统网站配置中的所有 default list 都必须作为运行时系统配置加载。 |
| D-029 | Stats Storage Foundation 已确认执行（初始） | Superseded by D-030 | 原始用量统计数据与分类解释必须分离存储和计算。`daily_usage_stats_v1` 初始设计为持久化的事实源。`event_log_v1` 是短期恢复 trace（24 小时保留）。决策要点：本地保留 365 天、日期使用本地日历日期、三个时长轨道、分类在读取时计算、五阶段实施。已被 D-030 取代：`daily_usage_stats_v1` 现在是物化聚合；`usage_segments_v1` 是核心持久事实账本。详见 `docs/STATS_STORAGE_FOUNDATION.md`。 |
| D-030 | `usage_segments_v1` 是核心持久事实账本 | Active | `usage_segments_v1` 始终是必需的——它是已完成持续时间的每个 segment 记录的持久化、不可变账本。之前缺失于文档和代码中，现已添加。`daily_usage_stats_v1` 是物化聚合，从 segments 派生，不应独立写入。同步顺序：segments 先于 aggregates 上传到云端（云端可以从 segments 重建 aggregates）。E2E 需要 segments 用于未来分析和验证。执行和 V1 新功能延迟直到 Stats Storage Foundation 在线且经过验证。详见 `docs/STATS_STORAGE_FOUNDATION.md`（修订版）。决策要点：1）单个结算路径：duration → segment → aggregate → outbox（一次性）；2）segment schema：id、profileId、deviceId、date、timezone、startMs、endMs、durationSeconds、domain、channel、mode、sourceState、settlementReason、parentSegmentId、partIndex/Count；3）云端 `usage_segments_v1` 表使用 `ON CONFLICT(id) DO UPDATE` 进行 idempotent upsert；4）五阶段实施（settlement + segments → read path → segment upload → aggregate upload → reconciliation）；5）不进行 pre-foundation 数据恢复。|
| D-031 | Cloud Stats 保留策略 | Active | Cloud `usage_segments_v1` 默认保留 **2 年**（730 天）。Cloud `stats_v1` 保留至少相同期限，除非 Product Owner 更改策略。在单独设计和批准之前，不实现自动删除/清理作业。无 Cron 触发器、无 D1 `DELETE` 语句、无基于保留期的作业。本地 `daily_usage_stats_v1` 和 `usage_segments_v1` 保留 365 天。详情见 `docs/STATS_STORAGE_FOUNDATION.md` §13。|
| D-032 | Cloud Sync 分离与调度策略 | Active | Stats Storage Foundation 的配置下载与数据上传必须分离为独立的同步子系统。六个子系统：Config Pull（cloud→terminal, 5min）、Quota State Pull（cloud→terminal, 5min）、Heartbeat（terminal→cloud, 5min）、Usage Segment Upload（terminal→cloud, 5min+dirty debounce）、Daily Stats Upload（terminal→cloud, 15min）、Legacy Stats Upload（terminal→cloud, 15min, 过渡）。每个子系统独立报告成功/失败、独立重试（指数退避 1→5→15→30→60 min）、独立 blocked/unbound 状态。Outbox 数据仅在远端幂等接受后才清除。Legacy `/device/stats` 必须在 Phase 3 改为 safe upsert；旧版 R2 sessions 不是 segment API。详情见 `docs/STATS_STORAGE_FOUNDATION.md` 附录 B。|
| D-033 | 发布策略切换：V0 baseline 冻结，首次正式发布目标改为 V1-minimal RC | Active | 自 2026-05-07 起，V0 不再作为 formal release 版本；V0 仅作为 internal stabilization baseline 并保留既有 release evidence（mode-switch lifecycle hardening、admin subpage refresh、local stats message path、timeline path、config cloud sync、cloud sync read-only evidence）。已知风险保留：legacy cloud `stats` 表存在重复行且缺少 `UNIQUE(profile_id,date,domain)`，因此 legacy cloud stats 不是干净单一真值源。`statsFoundationV1SyncEnabled` 继续保持 disabled，不纳入 V0 默认启用。新发布目标为 V1-minimal release candidate，聚焦发布就绪审计与真实设备 smoke 证据；不包含三模式模型重构、AI 分类、composite routing 重建、legacy D1 清理迁移、历史回填与分类策略扩展。不得据此宣称 V1-minimal 已 ready，需按新 gate 矩阵逐项放行。 |
| D-034 | V1-minimal 排除当前时间借用实现，借用需求迁移后续重设计 | Active | 自 2026-05-07 起，V1-minimal 发布范围不包含当前 `borrow time / borrow quota` 运行时实现路径（包括 message `BORROW_REST_QUOTA` 对应的现有交互链路）。该决策已按 docs + tests 证据落账：`BORROW_REST_QUOTA` 返回 `{ ok:false, error:"TIME_BORROWING_DISABLED_FOR_V1_MINIMAL" }`，且不改变 `quotaBorrow` 与 rest/weekly quota state。原始借用需求保留，不删除，但迁移到 post V1-minimal 版本重设计。后续必须重新定义：借用来源、借用目标、配额扣减与归还、审批/确认、UI 入口、云端字段、统计归因、与 Rest/Composite/Restricted Entertainment 的关系。在该重设计完成并单独批准前，不得将现有 borrow 实现作为 V1-minimal release gate 通过条件。 |
| D-035 | V1-minimal 启用 Cloud Stats v1 minimal sync gate | Active | 自 2026-05-08 起，V1-minimal 的 active stats cloud truth path 切换为 `usage_segments_v1` + `stats_v1`。`syncNow()` 在监控开启时优先执行 `syncStatsFoundationV1(enabled=true)`；legacy `/device/stats` 保留兼容但不再作为 release truth source。本次不执行 legacy 历史清理、不加 legacy UNIQUE 迁移、不做历史回填。`cloud_device_id` 通过 bind/config 响应持久化到本地；若缺失，v1 ingest 可由 device_token 解析设备 ID 并保持幂等。 |
| D-036 | Recovery/System manual evidence close-out 口径 | Active | 自 2026-05-08 起，Recovery/System Gate 可在 manual gates 由 Product Owner 完成并确认语义通过后，以 `MANUAL_VERIFIED_PASS` 落账。该口径必须显式标注 `operatorConfirmed=true`、`confirmedByOperator=true`、`automatedMeasurement=false`，且保留自动化事实（例如 sleep automated report=PARTIAL）不得删除或伪装为自动 PASS。close-out 报告须声明：仅 Gate.Test profile、无 product code 变更、无 Cloud/D1 写入、无 Worker 部署、无 production profile 触碰。 |
| D-037 | Mode-transition PiP cleanup 与 Study prompt 作为独立 V1-minimal regression gate | Superseded by D-041 | 自 2026-05-08 起，Rest->Composite / Rest->Study 的 PiP cleanup 与 entering-Study in-page prompt 归类为独立 mode-transition UX / side-effect regression gate，不并入 Recovery/System（sleep-wake）gate。发布前必须覆盖 manual/auto 四路径：Rest->Composite manual+auto、Rest->Study manual+auto，并回归 prompt lifecycle。`debugTriggerAutoTransition` 仅允许作为 E2E harness（sender/test-only 约束），不得作为普通用户路径。 |
| D-038 | Admin/bind account-token persistence package 纳入 V1-minimal follow-up candidate | Active | Product Owner 于 2026-05-09 批准 `admin/admin.js` + `bind.js` account-token persistence package 进入 include 路线，作为 V1-minimal follow-up candidate。证据：Build&Test implementation review、static minimum verification、focused unit test package `tests/unit/admin-bind-account-token.test.js`，结果 `5/5 PASS`。该决策不等同于 V1-minimal release-ready，不授权 rebuild/package/commit/push/tag/release。已知残余：`CLOUD_LOGOUT` 覆盖为静态断言而非完整 runtime message-router 调用；普通 admin auto-login failure 后 stale-token 语义另列 follow-up，不阻塞本包 include。 |
| D-039 | V1-minimal artifact strategy A：保持已提交 CWS 包作为 active review artifact | Active | Product Owner 于 2026-05-09 选择 artifact strategy A：已提交的 reduced-permission CWS package 继续作为 active review artifact（`dist/cws-resubmit-20260509-122919/timeonchrome-v1.7.2-cws-resubmit-minimal-permissions.zip`，SHA256 `BE0F712285B6661C293175C649DDDC48E0D04217B18626EB3C284EEAB32DD71C`）；当前 `origin/master` 作为 source follow-up line。除非 CWS 要求或 Product Owner 后续单独批准，不执行 rebuild/package/resubmission。该决策不等同于 public release ready；production-profile readonly smoke、CWS review outcome、Windows/macOS smoke、public release decision 与 git tag 仍需单独关闭。 |
| D-040 | Foreground timing 参数发布前重新确认 | Active | 当前前台网页计时实现采用：`periodic_checkpoint = 180s` 作为长 session 容错/切片窗口，`foreground boundary stabilization = 1s` 作为 tab/window/domain 切换防抖窗口。二者是当前开发阶段参数，不视为永久产品常量。后续发布、打包或 CWS 提交前，Product Owner 需重新确认关键时间选择，至少包括：checkpoint 180s 是否保留、tab 切换容错 1s 是否保留。 |
| D-041 | 当前版本全局禁止 PiP | Active | PiP 被定义为当前版本的管控漏洞，不作为受支持使用方式。任何 `isPiP === true`、open `pip` media session、media checkpoint 或 mode boundary 都必须尝试共享 `EXIT_PIP` cleanup；cleanup 成功写 `pip_forbidden_cleanup`，失败保留真实 `pip` 事实并记录诊断。未来是否允许学习/复合网站 PiP，以及如何纳入统计、配额、云端、家长端展示，是正式发布前必须重新设计的 release blocker。 |
| D-042 | 未归类网站入口改为“申请网站归类” | Superseded by D-049 | 历史规则：孩子侧曾把自动 pending 与手动“申请网站归类”视为同一类申请。D-049 已将系统自动事实记录与孩子主动学习归类申请拆分；审批前路由与硬拦截优先级仍延续本决策。 |
| D-043 | Popup 当前访问 fast path 需要后续复盘简化 | Pending PO | 当前为修复“正常页面显示不计时页面”，Popup 使用 active tab hint + background snapshot + `lastAccessed` 展示兜底。该方案功能上可用，但实现可能偏复杂。后续需重新讨论并收敛：Popup 当前访问的数据源应由 runtime session、active tab URL、还是单独轻量 read model 提供；未刷新 tab / service worker 冷启动 / 无 open session 时应如何展示；是否保留 `lastAccessed` 临时估算。复盘前不继续扩大该 fast path 复杂度。 |
| D-044 | Client Logging Foundation v1 归属与上传策略 | Active | 客户端日志作为可观测性基础设施，不改变计时、访问控制、模式切换、配额、同步、Popup 或 Admin 主流程；任何日志写入、查询、上传失败都必须静默降级。日志必须以 `profileId` 与 `deviceId` 作为一等归属字段；未绑定阶段可本地记录为 `null` + `bindingState=unbound`。本地默认记录 warning/error，保留 7 天、最多 1000 条；云端默认不上传，必须通过 `clientLoggingPolicyV1` 按 device/category/level/TTL 临时开启；info 上传必须有 TTL。日志必须脱敏，禁止 token/JWT/cookie/password/完整邮箱/完整 URL path/query/孩子姓名/本地路径/密钥进入本地或云端日志。 |
| D-045 | ManagedTarget 统计账本身份模型升级 | Active | 普通统计和配额归属从 `domain-only` 升级为 `managedTarget + fallback domain`。`domain` 继续作为事实、诊断和兼容字段；`managedTarget` 是显式配置的管理对象，不等同于所有浏览 URL。第一阶段已实现 resolver、`usage_segments_v1` 开账/切片 target 与 `quotaBucketAtTime` 快照、本地 daily/hourly `targets` 聚合、云端 segment 快照字段、`target_stats_v1` / `hourly_target_stats_v1` 和 admin/Pages target-first 读取。历史不按当前规则重解释。归集优先级：playlist > standalone video / explicit URL（无 playlist 上下文时）> platform entry > subdomain > domain > unmanaged domain fallback。同一精确 target 不能跨分类冲突，更具体 target 可以覆盖父 target。未命中显式 target 时不得保存完整 URL。详见 `docs/MANAGED_TARGET_LEDGER.md`。 |

| D-046 | 待归类时间与复合网站语义修订 | Active | 自 2026-07-24 起，用户可见概念统一为：综合时间 -> 待归类时间，综合网站 -> 复合网站，综合模式 -> 复合模式。待归类时间不是学习/休息之外的第三类最终时间，而是尚未实时或半实时归入学习或休息的过渡归因池；未来应尽量通过内容、URL、标题、频道、行为、AI 或家长回看归因到学习时间或休息时间，无法可靠判断时才保留 pending。复合网站是仅凭域名无法判断使用性质、需要内容级或行为级判断的网站；未归类网站仍是 fallback 状态，可能是潜在学习、娱乐、复合或应阻断对象，不自动等同复合网站。本决策不改代码字段、schema、API 或运行逻辑；`composite*` / `undetermined*` 继续作为 legacy/internal implementation value。 |

| D-047 | 文档分层与历史归档规则 | Active | 自 2026-07-24 起，当前工作入口只保留仍对产品语义、工程实现、部署、发布门禁或 agent 协作有约束力的文档；旧 audit、旧 release evidence、旧 smoke checklist、旧 handoff、旧计划、旧 PRD/TODO 统一归入 docs/archive/。Archive 文件只作为历史证据和追溯材料，除非 Product Owner 明确要求查历史，不得作为当前产品、工程、部署或发布 source of truth。当前权威清单以 AGENTS.md §9、PROJECT_MASTER.md、TASK_BOARD.md、DECISIONS.md 与当前 docs/ 权威文档为准。 |

| D-048 | 未归类网站自动进入待归类流程 | Active | 自 2026-07-24 起，未归类网站不是普通休息目标，而是待归类入口。访问控制观察到未命中学习/复合/受限娱乐/黑名单/已拒绝规则的网站时，必须自动创建或复用 `site_classification_requests_v1` pending 记录，默认粒度为 host，并在本次访问中按内部 `pending_composite` 路由进入待归类时间。该自动对象的产品名由 D-049 明确为“未归类网站访问记录”，不是孩子提交的归类申请。休息模式打开未归类网站也应进入待归类流程，不直接计入休息时间；只有待归类配额耗尽且休息可用时才 fallback 到休息。受限娱乐网站和已拒绝对象继续走休息/受限路径，不允许自动 pending。 |

| D-049 | 自动访问记录与手动学习归类申请拆分 | Active | 自 2026-07-24 起，系统自动 pending 的用户可见名称统一为“未归类网站访问记录”，表示系统观察到未归类网站被使用并提供给家长检查；孩子手动入口统一为“申请归为学习网站”，表示主动请求家长将目标归为学习网站。两者审批前都继续按内部 `pending_composite` 进入待归类时间，但不得在 UI 或产品文档中混称为同一种申请。同一有效 pending 目标只保留一条主记录：已有自动记录时，手动申请升级同一记录并保留访问概况，不创建重复项。访问概况聚合首次访问、最近访问和顶层导航次数；tab 激活、后台重检与心跳不累计。内部 storage key、审批接口和 `pending_composite` 值保持兼容。 |

| D-050 | 系统访问配置云端化与 Qustodio 分类管理 | Active | 自 2026-07-26 起，系统配置网站不再只由代码固定 JSON 表达；运行时优先读取云端 D1 的全局 `system-access-config`，代码中的 `workers/config/site-access-defaults.json` 仅作为初始化和 fallback。系统访问配置导入后全局生效，影响所有 profile 的 effective 清单；普通档案备份/恢复只处理 profile 自定义配置，不得修改系统配置。系统管理界面按 Qustodio Web Filters 风格维护内容类别，再映射到 TimeOnChrome 运行分类：教育/技术等默认学习，搜索/新闻/综合门户默认复合或待归类观察，娱乐/体育/游戏/购物/论坛/社交/聊天/视频默认受限娱乐，高风险内容默认黑名单。固定例外包括 YouTube 特殊网站对象管理（根域受限、具体对象可学习/复合）、Stack Overflow/Stack Exchange 学习、Reddit 复合、Kahoot/Quizizz 课堂工具、Douyin/TikTok 黑名单。 |
| D-051 | 网站管理使用管理策略目录与已使用未归类入口 | Active | 自 2026-07-26 起，家长控制台网站管理主分类固定为 TimeOnChrome 管理策略分类：学习、复合、受限娱乐、黑名单；Qustodio 内容分类只作为系统配置元数据。复合网站策略下新增“已使用未归类网站”入口，来源为最近 30 天未归类/待归类使用历史并经过当前 effective 清单过滤，不等同于审批记录或已确认复合网站。点击网站可移动分类：自定义项只改当前 profile，已使用未归类项写入当前 profile custom list 并关闭匹配 pending 记录，系统配置项仅管理员可经全局确认写入系统访问配置。 |
| D-052 | defaultUserCompositeSites 必须作为运行时系统复合配置加载 | Active | 自 2026-07-27 起，系统网站配置中的所有 default list 都是运行时 source of truth：`defaultStudySites`、`defaultCompositeSites`、`defaultUserCompositeSites`、`defaultRestrictedEntertainmentSites`、`defaultBlockedSites`。`defaultUserCompositeSites` 不再仅作为新建 Profile 种子或推荐字段；Worker effective config、扩展端配置归一化、分类解析、父域保护、Popup/Admin 展示都必须把它作为复合网站系统来源加载。受限娱乐和黑名单优先级继续高于复合；本决策不把 YouTube 改为学习，不改变未归类网站访问路由，不迁移历史统计。 |
| D-053 | YouTube 进入特殊网站对象管理 | Active | 自 2026-07-27 起，`youtube.com` 根域不再作为复合网站默认入口，而是系统受限娱乐网站。具体 YouTube 视频、播放列表、频道是特殊网站对象，可由家长批准为学习或复合，并可在 `youtube.com` 受限父域下例外生效。孩子端仍只提交学习申请；家长审批时可改判学习或复合。频道规则可覆盖该频道下视频，但只有当页面上下文能识别频道对象时生效；`music.youtube.com` 本轮不变。 |
| D-054 | YouTube 根域系统配置不变量与时间段边界定时评估 | Active | 自 2026-07-29 起，系统访问配置读取必须修正旧 D1 或导入文件中的 YouTube 根域漂移：`youtube.com` 不得作为学习、复合、用户默认复合或黑名单默认项加载，必须作为系统受限娱乐根域加载；具体 YouTube 视频、播放列表、频道仍按 D-053 的特殊对象规则例外处理。时间段管理不只在访问新网站或手动切换时生效，定时/心跳触发的 `EVALUATE_QUOTA_STATE` 也必须检查当前模式是否仍在允许时间段内，并在越界时切换到当前可用模式或进入 locked 后重检 active tab。 |
| D-055 | 网站访问运行时配置必须版本化归一化 | Active | 自 2026-07-29 起，扩展运行时不得直接信任历史 `guardian_config` 中的 effective 清单或 legacy aliases。任何来自本地缓存、云端拉取、导入/恢复、绑定首次同步或云端 version skip 的网站访问配置，都必须先经过 `normalizeRuntimeSiteAccessConfig()` 升级到当前语义版本，再进入分类、拦截、计时和 managed target 落账。`studyList`、`compositeList`、`restrictedEntertainmentList`、`unsafeList` 是由 source lists 派生的 canonical effective 清单；legacy aliases 只允许在 migration/normalization 层读取。YouTube 根域受限娱乐只是 M002 迁移规则，不得作为散落在分类器和落账器里的长期硬编码。 |
| D-056 | 任务管理 V1 进入隔离分支实现 | Active | 自 2026-08-05 起，任务管理 V1 产品规格与技术设计批准进入实现，目标分支为 `codex/task-management-v1`。第一版只支持一次性任务，不支持周期任务或固定截止时间；任务以计划开始时间、要求有效使用时长和允许资源为核心，开始或产生进度后核心字段冻结。任务是高优先级 Chrome 内容策略：`安全与黑名单 > 未完成任务 > 配额 > 日历例程/旧时间段 > 基础场景`；任务命中资源时可覆盖旧时间段限制，但不改变网站性质、统计性质或配额来源，也不绕过黑名单、安全规则或配额耗尽。任务进度以 `usage_segments_v1` 为唯一事实，`tasks_v1.completed_seconds` 只是可重建投影；多设备按有效使用区间并集累计，同一自然秒最多计一次。实现包必须保持隔离，不执行生产 D1 migration、不部署生产环境、不开放 Pages 创建入口，除非 Product Owner 后续单独授权。 |
| D-057 | 任务管理 V1 隔离分支实现与测试收口 | Active | 自 2026-08-05 起，`codex/task-management-v1` 已完成任务管理 V1 的 P1-P9 实现与本地验证：D1 migration/repository、Worker parent/device API 与 capability、扩展 task cache/pull/alarm、运行时 segment 任务快照、Worker 进度投影、Popup/Admin/Reminder 只读展示、Pages 任务管理入口与 capability gate、runtime `task_required` 阻断、本地集成与回滚验证均已在隔离分支实现并推送。Pages 创建入口只在该隔离分支代码中存在，尚未部署到生产；生产 D1 migration、Worker/Pages 部署、扩展版本升级、托管发布和生产 profile 启用仍需 Product Owner 另行批准并按 release gate 执行。 |

## 变更规则
- 新决策必须追加一条记录（不改历史 ID）。
- 决策状态：`Active / Superseded / Dropped / Pending PO`。
